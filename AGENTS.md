# Repository Guidance

- Keep changes narrowly scoped to the user's request.
- For non-trivial new or changed helper functions, add or update concise `##` doc comments that explain the function's intent and any important behavior.
- Avoid adding comments for obvious one-line logic or simple pass-through functions.
- When changing a function's purpose, update nearby comments/doc comments so they match the new behavior.
